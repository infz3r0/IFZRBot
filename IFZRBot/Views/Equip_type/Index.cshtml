@model IEnumerable<IFZRBot.Models.equip_type>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equip_type_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equip_type_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.equip_type_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.equip_type_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.equip_type_id })
        </td>
    </tr>
}

</table>
